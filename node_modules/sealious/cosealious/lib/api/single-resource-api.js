"use strict";

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var axios = require("axios");
var CachedHttp = require("../cached-http.js");
var EventEmitter = require("events");

module.exports = function (_EventEmitter) {
	_inherits(SingleResourceAPI, _EventEmitter);

	function SingleResourceAPI(collection_name, id, options) {
		_classCallCheck(this, SingleResourceAPI);

		var _this = _possibleConstructorReturn(this, (SingleResourceAPI.__proto__ || Object.getPrototypeOf(SingleResourceAPI)).call(this));

		_this.id = id;
		_this.collection_name = collection_name;
		_this.data = null;
		_this.loading = true;
		_this.filter = options && options.filter || {};
		_this.format = options && options.format || {};
		return _this;
	}

	_createClass(SingleResourceAPI, [{
		key: "load",
		value: function load() {
			var _this2 = this;

			this.loading = true;
			return CachedHttp.get("/api/v1/collections/" + this.collection_name + "/" + this.id, { filter: this.filter, format: this.format }).then(function (response) {
				_this2.loading = false;
				_this2.data = response;
				_this2.emit("change", response);
			});
		}
	}, {
		key: "setLoading",
		value: function setLoading(loading) {
			this.loading = loading;
			this.emit("change", this.data);
		}
	}, {
		key: "patch",
		value: function patch(body) {
			var _this3 = this;

			this.loading = true;
			return axios.patch("/api/v1/collections/" + this.collection_name + "/" + this.id).then(function (response) {
				_this3.loading = false;
				_this3.data = response;
				_this3.emit("change", response);
			});
		}
	}]);

	return SingleResourceAPI;
}(EventEmitter);